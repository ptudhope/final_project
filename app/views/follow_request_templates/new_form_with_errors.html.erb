<% if @follow_request.present? %>
  <% if @follow_request.errors.any? %>
    <% @follow_request.errors.full_messages.each do |message| %>
      <div class="alert alert-danger">
        <%= message %>
      </div>
    <% end %>
  <% end %>
<% end %>

<h1>
  Add a new follow request
</h1>


<hr>

<form action="/create_follow_request" method="post">

  <div class="form-group">
    <label for="status">
      Status
    </label>

    <input type="text" id="status" name="status" class="form-control" value="<%= @follow_request.status %>">
  </div>

  <div class="form-group">
    <label for="recipient_id">
      Recipient
    </label>

    <input type="text" id="recipient_id" name="recipient_id" class="form-control" value="<%= @follow_request.recipient_id %>">
  </div>

  <div class="form-group">
    <label for="sender_id">
      Sender
    </label>

    <input type="text" id="sender_id" name="sender_id" class="form-control" value="<%= @follow_request.sender_id %>">
  </div>

  <button class="btn btn-block btn-outline-secondary">
    Create follow request
  </button>
</form>

<hr>

<a href="/follow_requests">
  Go back
</a>
